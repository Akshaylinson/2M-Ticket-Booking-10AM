# Backend Package

This directory contains the FastAPI application, SQLAlchemy models, and tests for the ticket booking platform.

The canonical project overview lives at the repository root `README.md`.
